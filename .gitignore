node_modules/
out/
dist/
.DS_Store
*.dmg
*.zip
.env
*.env
*.env.*
.env.*
!.env.example
build/*.original.icns

# Local-only AI/dev notes — keep on disk for Claude Code project memory and
# personal next-step tracking, but don't expose internal handoff state in
# the public repo. Future clones won't see this; that's intentional.
/CLAUDE.md
/DEVLOG.md
